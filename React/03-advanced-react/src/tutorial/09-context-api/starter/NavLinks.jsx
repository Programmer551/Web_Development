import UserContainer from "./UserContainer";
const NavLinks = ({ logout, user, login }) => {
  return (
    <>
      <div>NavLinks</div>
      <UserContainer
        // user={user}
        // logout={logout}
        // login={login}
      />
    </>
  );
};
export default NavLinks;
